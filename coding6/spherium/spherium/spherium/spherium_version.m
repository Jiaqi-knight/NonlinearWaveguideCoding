%spherium_version
% Function which defines current version (string). This is the ONLY place
% where the authentic version is stored within the code!
function v = spherium_version

v = {
    'Andy French',...
    'v2.1 2013'
    };
v = char(v);

%End of code